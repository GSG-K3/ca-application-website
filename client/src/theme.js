import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
	palette: {
		primary: { main: '#4CB7A7' },
		secondary: {
			main: '#FB6C04',
		},
		colors: {
			backgroundColor: '#000',
		},
	},
	spacing: 4,
});
export default theme;
